describe("Checking flow", () => {
  beforeEach(() => {
    cy.visit("");
  });

  it("Testing checking", () => {
    cy.fixture("test.json").then((data) => {
      data.forEach((dataset) => {
    
        
      });
    });
  });
});
